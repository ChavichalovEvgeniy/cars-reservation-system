export enum ParamTypes {
  Body = 'body',
  Params = 'params'
}

export enum Status {
  ERROR = 'Error',
  SUCCESS = 'Success'
}

export enum HttpStatus {
  InternalServerError = 500,
  Conflict = 409,
  NotAcceptable = 406,
  NotFound = 404
}
